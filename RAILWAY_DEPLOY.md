# 🚀 Развёртывание на Railway.com

## ✅ Проект готов к деплою на Railway

Этот проект полностью настроен для развёртывания на платформе [Railway](https://railway.app).

---

## 📋 Файлы для деплоя

Проект содержит все необходимые файлы для автоматического развёртывания:

| Файл | Назначение |
|------|------------|
| `Procfile` | Указывает Railway команду запуска: `worker: python main.py` |
| `requirements.txt` | Список зависимостей Python |
| `runtime.txt` | Версия Python (3.11.8) |
| `.env.example` | Шаблон переменных окружения |

---

## 🔧 Инструкция по развёртыванию

### Шаг 1: Подключите репозиторий к Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий

### Шаг 2: Настройте переменные окружения

В панели Railway перейдите в **Variables** и добавьте:

```bash
# ===== ОБЯЗАТЕЛЬНЫЕ =====
BOT_TOKEN=your_telegram_bot_token
LZT_TOKEN=your_lzt_market_api_token
ADMIN_IDS=123456789,987654321

# ===== ОПЦИОНАЛЬНЫЕ =====
CHANNEL_ID=@your_channel
CRYPTOBOT_TOKEN=your_cryptobot_token_here
CRYPTOBOT_TESTNET=false
API_TIMEOUT=15
LOG_LEVEL=INFO
DATABASE_URL=sqlite:///data/shop.db
```

> ⚠️ **ВАЖНО для CryptoBot:** Ошибка HTTP 401 означает неверный токен. 
> 1. Получите токен в CryptoBot: https://crypt.bot/app/crypto_bot → Настройки → Разработчикам → Создать токен
> 2. Убедитесь, что скопировали токен без лишних пробелов
> 3. Проверьте, что используете правильный токен (не от тестовой сети, если CRYPTOBOT_TESTNET=false)

### Шаг 3: Запуск

Railway автоматически:
- Установит зависимости из `requirements.txt`
- Запустит бота через `Procfile`

---

## 🗄️ База данных

### Вариант 1: SQLite (по умолчанию)
Используется файл `data/store.db`. Для сохранения данных между перезапусками:

1. В панели Railway создайте Volume:
   - Путь: `/workspace/data`
   - Размер: 1 ГБ (минимум)

### Вариант 2: PostgreSQL (рекомендуется для продакшена)
1. Добавьте PostgreSQL плагин в Railway
2. Скопируйте `DATABASE_URL` в переменные окружения
3. Обновите `DATABASE_URL` в настройках бота

---

## 📝 Логи

Логи бота доступны:
- В панели Railway → **Deployments** → **View Logs**
- В файле `logs/bot.log` (внутри контейнера)

---

## 🔍 Мониторинг

### Проверка работоспособности
1. Отправьте боту `/start` — должен ответить приветствием
2. Проверьте логи на наличие ошибок
3. Протестируйте поиск аккаунтов через кнопку "Каталог"

### Переменные для проверки
```bash
# Включите подробное логирование
LOG_LEVEL=DEBUG
```

---

## 🛠️ Решение проблем

| Проблема | Решение |
|----------|---------|
| Бот не запускается | Проверьте `BOT_TOKEN` и `LZT_TOKEN` в переменных |
| Ошибка "ModuleNotFoundError" | Убедитесь что все зависимости в `requirements.txt` |
| База данных не сохраняется | Создайте Volume для `/workspace/data` |
| Таймауты API LZT | Увеличьте `API_TIMEOUT` до 30 |
| **CryptoBot HTTP 401** | Проверьте `CRYPTOBOT_TOKEN`: нет ли пробелов, правильный ли токен, соответствует ли тестовой сети (`CRYPTOBOT_TESTNET`) |
| CryptoBot счёт не создаётся | Включите `LOG_LEVEL=DEBUG` и проверьте логи в панели Railway |

---

## 🔄 Обновление проекта

После пуша в GitHub Railway автоматически:
1. Обнаружит изменения
2. Пересоберёт проект
3. Перезапустит бота

---

## 📊 Тарифы Railway

- **Hobby**: $5/месяц — достаточно для тестового бота
- **Pro**: от $20/месяц — для продакшена с большим трафиком

---

## 🔐 Безопасность

- ⚠️ **Никогда не коммитьте `.env` файл!**
- Все чувствительные данные храните в переменных окружения Railway
- Используйте `.gitignore` для исключения чувствительных файлов

---

## 📞 Поддержка

При проблемах с развёртыванием:
1. Проверьте логи в панели Railway
2. Убедитесь что все переменные окружения заданы
3. Протестируйте локально перед деплоем
